# Application
Todo list

## Install maven

To run the project, you need maven installed.
First install the dependencies with following command:

```
mvn install
``` 

## Usage

Compile the project:

```
mvn compile
```

Run the project:

```
mvn javafx:run
```